# Other

## Real heading

This file's only `##` heading slug is `real-heading`. The fixture
`README.md` links here twice — once with a real fragment, once with a
bogus one (which is what the `md-link-fragments` rule catches).
